// vue.config.js
// 这是脚手架的配置文件, 并非必须
module.exports = {
    // 如果你想修改脚手架的 webpack 配置
    // 可以设置这 configureWebpack 属性
    // 所有这里的配置, 会覆盖 脚手架的默认设定
    configureWebpack: {
        //添加或更改的webpack配置
    }
}